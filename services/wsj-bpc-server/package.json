{
  "name": "bpc-headless-server",
  "private": true,
  "version": "0.1.0",
  "description": "Headless Chromium + Bypass Paywalls Clean: fetch paywalled articles via a small HTTP API",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify": "node verify.mjs",
    "start": "node index.mjs"
  },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
