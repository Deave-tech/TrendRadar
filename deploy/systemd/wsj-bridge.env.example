# Optional: install as /etc/trendradar/wsj-bridge.env with root:root mode 0600.
# Set only the DataDome token below; the bridge adds the "datadome=" prefix.
WSJ_CN_USER_AGENT=
WSJ_CN_DATADOME_COOKIE=
