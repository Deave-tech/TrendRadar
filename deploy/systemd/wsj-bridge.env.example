# Required: install as /etc/trendradar/wsj-bridge.env with root:root mode 0600.
# Use the same API token as /etc/trendradar/bpc-server.env.  The bridge never
# receives a WSJ Cookie or User-Agent; BPC owns the single browser session.
BPC_BASE_URL=http://127.0.0.1:8080
BPC_API_TOKEN=
